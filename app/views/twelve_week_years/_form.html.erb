<%= form_with(model: twelve_week_year, local: true) do |form| %>
  <% if twelve_week_year.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twelve_week_year.errors.count, "error") %> prohibited this twelve_week_year from being saved:</h2>

      <ul>
      <% twelve_week_year.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :twelve_week_year_name %>
  </div>

  <div class="field">
    <%= form.label :starts_on %>
    <%= form.date_select :starts_on, id: :twelve_week_year_starts_on %>
  </div>

  <div class="field">
    <%= form.label :ends_on %>
    <%= form.date_select :ends_on, id: :twelve_week_year_ends_on %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
